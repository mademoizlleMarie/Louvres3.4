services:
#    marie_louvres.example:
#        class: Marie\LouvresBundle\Example
#        arguments: ["@service_id", "plain_value", "%parameter%"]
services:
    BookingManager:
        class: Marie\LouvresBundle\Service\BookingManager

        #revoir pk c'est écrit de cette manière ca depend de la version de symfony
        arguments: ['@session']
